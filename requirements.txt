# Core runtime dependencies — pinned to the exact versions the release was
# tested against, so runs are reproducible. Bump deliberately, then re-run the
# test suite (see requirements-dev.txt).
requests==2.34.2
tqdm==4.69.1
rapidfuzz==3.14.5
stix2==3.0.2
